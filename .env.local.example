# AgentTailor — Local Mode (no external API keys required)
# Copy this to .env: cp .env.local.example .env

# Mode Configuration
AUTH_MODE=local
EMBEDDING_PROVIDER=local

# Database (PostgreSQL via docker compose)
DATABASE_URL="postgresql://agenttailor:agenttailor_dev@localhost:5433/agenttailor_dev"

# Redis (via docker compose)
REDIS_URL="redis://localhost:6379"

# ChromaDB (via docker compose)
CHROMA_URL="http://localhost:8100"
VECTOR_STORE_PROVIDER="chroma"

# Server
NODE_ENV="development"
PORT="4000"
CORS_ORIGIN="http://localhost:5173"
